{
  "opencollection": "1.0.0",
  "info": {
    "name": "Get Bom Assembly Configuration API",
    "version": "3.0"
  },
  "items": [
    {
      "info": {
        "name": "Configuration",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Upsert a new configuration. A configuration is a custom setting or metadata within the package, the former being keyed by the 'Name' field whilst the latter on the 'Label' field.",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://{endpoint}/services/apexrest/{namespace}/v2/configuration/",
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Upsert a new configuration. A configuration is a custom setting or metadata within the package, the former being keyed by the 'Name' field whilst the latter on the 'Label' field."
        }
      ]
    }
  ],
  "bundled": true
}